# Dump: T007 — CharacterStatCalculator

## Session Info
| Field | Value |
|-------|-------|
| **Task** | T007 — CharacterStatCalculator |
| **User** | gal |
| **Date** | 2026-03-02 |
| **Branch** | `gal` (integration) — task branch not yet created |
| **Phase** | 1 — Foundation |

## Progress

### Status
**Planning complete. Execution not yet started.**

All 5 design decisions have been made and are documented in the task spec under `## Design Decisions`. No code files for T007 have been written yet. The session also included a design update that touched T006 files (see "Design Update" below).

### Acceptance Criteria
- [ ] Pure C# class, not a MonoBehaviour or ScriptableObject
- [ ] Correct formula: `(Base + PathBonuses) * Ritual * Trinket * SoulTree` applied per stat independently
- [ ] Returns `FinalStats` struct with all 10 stats
- [ ] Integer stats (HP, DEF, MNA) rounded after multiplication
- [ ] CritChance clamped to 0.0–1.0
- [ ] Default multipliers of 1.0 produce unmodified base stats
- [ ] `rangedAttack` is `-1f` for non-Viper with no modifiers applied
- [ ] `throwableAttack` calculated normally for all 4 characters
- [ ] `StatModifierInput.Default(baseStats)` produces neutral (unmodified) output
- [ ] `FinalStats.GetAttackForMode(AttackMode.Ranged)` returns `attack` for non-Viper (no `-1f` leak)
- [ ] `AttackMode` enum in `Shared/Enums/`
- [ ] Unit testable with no scene required
- [ ] Compiles with zero warnings

### Files (T007 — none created yet)
| File | Status |
|------|--------|
| `Assets/Scripts/Shared/Enums/AttackMode.cs` | Not started |
| `Assets/Scripts/Paths/FinalStats.cs` | Not started |
| `Assets/Scripts/Paths/StatModifierInput.cs` | Not started |
| `Assets/Scripts/Paths/CharacterStatCalculator.cs` | Not started |

## Design Update (completed this session — uncommitted)

Before T007 planning concluded, a design change was made to separate `RangedAttack` (Viper-only) from `ThrowableAttack` (all characters). These changes are **done but not yet committed** to either repo.

### Code repo (`gal` branch) — uncommitted changes
| File | Change |
|------|--------|
| `Shared/Enums/StatType.cs` | Added `ThrowableAttack` after `RangedAttack`; doc comment explains -1 sentinel |
| `Shared/Data/CharacterBaseStats.cs` | `rangedAttack` marked Viper-only (`-1` default); new `throwableAttack` field added |
| `ScriptableObjects/Characters/BrutorStats.asset` | `rangedAttack: -1`, `throwableAttack: 1.1` |
| `ScriptableObjects/Characters/SlasherStats.asset` | `rangedAttack: -1`, `throwableAttack: 1.0` |
| `ScriptableObjects/Characters/MysticaStats.asset` | `rangedAttack: -1`, `throwableAttack: 0.7` |
| `ScriptableObjects/Characters/ViperStats.asset` | `rangedAttack: 1.8` (unchanged), `throwableAttack: 1.1` |

### Docs repo (`gal` branch) — uncommitted changes
| File | Change |
|------|--------|
| `architecture/interface-contracts.md` | `StatType` enum updated: `ThrowableAttack` added, inline comments |
| `design-specs/CHARACTER-ARCHETYPES.md` | Stat framework, all 4 character tables, comparison table updated |
| `tasks/phase-1/T007-stat-calculator.md` | Design Decisions section added; FinalStats updated to 10 stats; File Plan updated |

## Blockers / Errors
No blockers reported. Planning is complete and all decisions are documented.

## Design Decisions Made (all binding for execution)

### DD-2: Directory — `Assets/Scripts/Paths/`
Calculator is queried by all 3 pillars, so it lives in neutral `Paths/` (not inside `Roguelite/`).
Namespace: `TomatoFighters.Paths`.

### DD-3: 3 separate files
`FinalStats`, `StatModifierInput`, and `CharacterStatCalculator` each get their own file for discoverability.

### DD-4: float[] arrays + `StatModifierInput.Default()` factory
Modifier inputs are `float[]` indexed by `StatType` (11 values). `Default(CharacterBaseStats)` initializes neutral values (0f additive, 1.0f multiplicative). `CancelWindow` slot is zeroed and silently ignored by the calculator.

```csharp
var input = StatModifierInput.Default(characterBaseStats);
FinalStats result = calculator.Calculate(input);
```

### DD-5: `FinalStats.GetAttackForMode(AttackMode)` helper
Absorbs the `-1f` sentinel internally so Combat/World never see it:

```csharp
public float GetAttackForMode(AttackMode mode) => mode switch
{
    AttackMode.Melee     => attack,
    AttackMode.Ranged    => rangedAttack >= 0f ? rangedAttack : attack,
    AttackMode.Throwable => throwableAttack,
    _                    => attack
};
```

`AttackMode` enum (`Melee`, `Ranged`, `Throwable`) goes in `Shared/Enums/` since Combat pillar needs it.

## Recommended Next Steps
1. **Commit the design update** in both repos before branching:
   - Code repo: `git add` the 6 modified files → commit `[Phase 1] Design: Separate RangedAttack (Viper-only) from ThrowableAttack (all characters)`
   - Docs repo: `git add` the 3 modified files → same commit message
2. **Create the task branch**: `git checkout -b pillar2/T007-stat-calculator`
3. **Execute**: `/task-execute T007` — all design decisions are binding, agent should follow them exactly
4. **Verify** `Assets/Scripts/Paths/` directory is created (doesn't exist yet in the Unity project)
