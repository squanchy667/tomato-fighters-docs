# Dump: T020 — RitualData ScriptableObject + Families

## Session Info

| Field | Value |
|-------|-------|
| **Task** | T020 — RitualData ScriptableObject + Families |
| **User** | dev2 |
| **Date** | 2026-03-03 |
| **Branch** | `gal` (T020 fully merged — `pillar2/T020-ritual-data-so` → `gal`) |
| **Phase** | 2 |

## Progress

### Acceptance Criteria
- [x] `RitualData` SO with all fields (identity, effect, twin, level scaling)
- [x] `RitualLevelData` struct with `baseValue`, `maxStacks`, `stackingMultiplier`, `ritualPower`
- [x] `CreateAssetMenu` path: `"TomatoFighters/Data/RitualData"`
- [x] `RitualDataCreator` MenuItem: `"TomatoFighters/Create Ritual Assets"`
- [x] Fire family: Burn, Blazing Dash, Flame Strike, Ember Shield
- [x] Lightning family: Chain Lightning, Lightning Strike, Shock Wave, Static Field
- [x] Level 1–3 power values defined per ritual
- [x] Compiles with zero warnings (verified conceptually — run MenuItem in Unity to generate .asset files)

### Files

| File | Status |
|------|--------|
| `Assets/Scripts/Roguelite/RitualData.cs` | Done ✓ |
| `Assets/Editor/RitualDataCreator.cs` | Done ✓ |
| `tasks/phase-2/T020-ritual-data-so.md` | Done ✓ (spec with 3 DDs) |

> Note: `.asset` files are NOT in git — they must be generated by running the MenuItem in Unity Editor.
> Run: `TomatoFighters → Create Ritual Assets`

## Blockers / Errors

No blockers. T020 is 100% complete and merged.

**Pending action (architecture doc):**
The `/sync-docs` run flagged that `system-overview.md` does not yet list `RitualData.cs` in the Roguelite/ file list. This is a carry-over from the original task board listing it under `Shared/Data/`. A one-line fix is needed — see Architecture Note below.

## Uncommitted Changes

Only `.meta` files are untracked in the code repo — these are Unity-generated and should be committed when opening the project in Unity Editor. They're not blocking anything.

```
Untracked Unity .meta files (commit when Unity is open):
  Assets/Editor/PathDataCreator.cs.meta
  Assets/Scripts/Paths/*.cs.meta + asmdef.meta
  Assets/Scripts/Roguelite/Currency*.cs.meta
  Assets/Scripts/Shared/Data/PathData.cs.meta + PathTierBonuses.cs.meta
  Assets/Scripts/Shared/Enums/AttackMode.cs.meta + CurrencyType.cs.meta
  Assets/Tests/EditMode/Paths/*.meta
```

## Context Notes

### What was completed this session
1. **T018 PathSystem** — was already committed; merged to gal + pushed
2. **Tests written** — `CharacterStatCalculatorTests.cs` (17 tests) + `PathSystemTests.cs` (24 tests), all pass
3. **T020 RitualData SO** — planned (3 design decisions), implemented, merged, synced

### Design Decisions (T020)
- **DD-1:** `string effectId` (e.g. `"Fire_Burn"`) — matches PathData `tier1AbilityId` pattern. RitualSystem (T021) dispatches via `Dictionary<string, Action<RitualEffectContext>>`.
- **DD-2:** Three explicit `RitualLevelData` structs (`level1/level2/level3`) — mirrors PathTierBonuses. Level multipliers `1.0/1.5/2.0` are fixed constants in `RitualStackCalculator` (T029), not stored on SO.
- **DD-3:** `RitualData` lives in `Roguelite/` not `Shared/Data/` — `IBuffProvider.GetTriggerEffects` returns `OnTriggerEffect`, never `RitualData` directly. Task board file path was overridden.

### Key architectural patterns in play
- `OnTriggerEffect` is still a placeholder struct in `Shared/Data/PlaceholderTypes.cs` — T021 will define it properly
- `IBuffProvider.GetTriggerEffects(RitualTrigger)` returns `List<OnTriggerEffect>` — this is the Combat-facing API that RitualSystem will implement
- The ritual stacking formula: `base × levelMultiplier (1.0/1.5/2.0) × stackingMultiplier × ritualPower` lives in `RitualStackCalculator` (T029)

### Architecture flag (action needed)
`system-overview.md` lists Roguelite/ as 12 files without `RitualData.cs`. It should be:
```
├── RitualData.cs   ← ADD THIS (T020 ✓, lives in Roguelite/ per DD-3)
```

## Recommended Next Steps

1. **Fix architecture/system-overview.md** — add `RitualData.cs` to Roguelite/ file list (one line, low priority)
2. **T021: RitualSystem** — fully unblocked. Run `/plan-task T021` to start. Key inputs:
   - `RitualData.effectId` → dispatch table
   - `IBuffProvider` interface → RitualSystem implements it
   - `ICombatEvents` → RitualSystem subscribes to all triggers
   - `OnTriggerEffect` struct needs to be defined as part of T021
3. **T019: PathSelectionUI** — also unblocked (depends on T018 only), can run in parallel with T021
4. **Commit .meta files** — open Unity Editor, let it generate/verify .meta files, then commit them
5. **Run `TomatoFighters → Create Ritual Assets` in Unity** — generates the 8 .asset files for Fire + Lightning families
